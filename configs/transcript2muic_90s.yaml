---
clip_duration: 90.

train_datasets:
    SymphonyVAE:
        root: "./datasets/youtube"
        split: "train"
        
test_datasets:
    SymphonyVAE:
        root: "./datasets/youtube"
        split: "test"
        
sampler: RepeatShuffleSampler

data_transform: 
    name: Trans2MusicVAE

base:
    name: Transformer1D
    in_dim: 64
    patch_size: 1
    dim: 768
    num_layers: 12
    num_heads: 12
    rope_len: 8192

adaptor:
    name: OnehotTransEncoder
    num_classes: 12
    in_channels: 128
    dim: 64

train:
    device: cuda
    num_workers: 16
    precision: "no"  # "no" (fp32) | "bf16"
    optimizer: AdamW
    lr: 1e-4
    warm_up_steps: 1000  # Leave blank if no warm up is used
    batch_size_per_device: 4
    test_every_n_steps: 10000
    save_every_n_steps: 50000
    training_steps: 500000
    resume_ckpt_path:  # Leave blank if train from scratch

valid_audios: 10